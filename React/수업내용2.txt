2월14일======================================================
3.termical에 원하는 폴더에 npm create react-app 프로젝트명(react-basic) 입력
5.컴포넌트는 재활용이 필요한 것들 (함수,클래스,js파일..)
6.최상위는 App.js 로 생각
7.props : 부모컴포넌트에서 자식컴포넌트로 내려주는 변수
8.import를 바로 할 때, 컨트롤+스페이스
9.state값 변경시 화면이 다시 그려진다. 리렌더링 진행!
10.setData() 함수는 비동기적인 실행이다(순서대로 실행되지 않는다)
11.useState() 는 배열을 반환하고 첫번째는 현재값, 두번째는 값을 변경하는 함수가 반환된다
